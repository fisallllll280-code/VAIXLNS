# VAIXLNS Multi-Mind + GitHub Federation

VAIXLNS now defines a provider-neutral multi-mind integration for VX.

Canonical authority remains outside the minds:
AI/tool -> Proposal -> Policy/Authority -> VX Execution -> VV Verification -> OIF Operational Proof

## Federated repositories
- fisallllll280-code/VAIXLNS
- fisallllll280-code/VAIXLNS-unified
- fisallllll280-code/vaixlns-core
- fisallllll280-code/vaixlns-csd-kernel
- fisallllll280-code/VAIXLNS-Intent-to-Reality
- fisallllll280-code/VX50_COMPLETE_BUILD
- fisallllll280-code/NEXENT

These are federated components, not a claim that all code has been merged.

## Free-first principle
The architecture can operate with local AI tools through the local command adapter. Paid APIs are optional adapters, not architectural dependencies.

## Operating loop
INTENT -> MULTI-MIND -> DESIGN -> BUILD -> TEST -> SECURITY -> POLICY GATE -> VX -> EVENT/STATE/LEDGER -> VERIFY -> PROVE -> OPERATE -> OBSERVE -> RECOVER -> EVOLVE